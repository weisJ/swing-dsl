name: Sonarlint

on: [push, pull_request]

jobs:
  sonarlint:
    name: "Sonarlint"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 10
      - name: Check
        run: ./gradlew sonarlintMain
